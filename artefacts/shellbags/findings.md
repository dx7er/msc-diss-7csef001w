# ShellBags Findings

Scenario-level findings from parsing ShellBags (from `NTUSER.DAT` and `UsrClass.dat`) collected from post-scenario snapshots.

Populated after pilot execution: parsed CSVs from SBECmd / ShellBags Explorer land alongside this file, and observations on browsed vs unbrowsed paths (positive vs negative controls) are recorded here.
